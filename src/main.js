// Updated content of src/main.js
import './capture';

// Existing content

// Handle IPC events
ipcMain.on('start-capture', (event, arg) => {
    // handle start-capture
});

ipcMain.on('select-output', (event, arg) => {
    // handle select-output
});
